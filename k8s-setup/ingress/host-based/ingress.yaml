# one ingress , two domain names
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: host-based-ingress
  annotations: 
    # ingress class name 
    # cluster issuer for (tls , https )
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    kubernetes.io/ingressClassName: "nginx"
spec:
  ingressClassName: nginx
  tls: 
    - hosts: 
      - ingress-nginx2.sethnexusonline.online 
      - ingress-earthdx2.sethnexusonline.online 
      secretName: ingress-host-tls
  rules:
  - host: ingress-nginx2.sethnexusonline.online 
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: nginx-service
            port: 
              number: 80
  - host: ingress-earthdx2.sethnexusonline.online 
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: reactjs-service
            port: 
              number: 80
 

 # nslookup earthdx-ingress.sethnexusonline.online 
 # nslookup nginx-ingress.sethnexusonline.online 